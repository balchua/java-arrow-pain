target/

# Test data — generated XML files and Arrow IPC output (too large to commit; regenerated by tests)
test-data/sample-data/*.xml
test-data/output/

# Test results (logs only)
test-results/*.log
*.log
